# Where the instance is reachable from the outside. Must end in a slash.
SEARXNG_BASE_URL=http://localhost:8080/

# The one host port. Serves the token gate by default, or SearXNG itself when
# running with docker-compose.unguarded.yml. The token is written to token.txt
# on the first run.
SEARXNG_PORT=8080
