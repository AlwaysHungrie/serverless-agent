{
  "name": "admin-cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive CLI dashboard for the salt-agent deployment: counts, the limit-increase queue, and the deployment settings.",
  "scripts": {
    "start": "node index.mjs",
    "dev": "node index.mjs --dev"
  }
}
